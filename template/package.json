{
    "name": "{{ name }}",
    "version": "1.0.0",
    "description": "{{ name }}",
    "scripts": {
        "start": "rm -rf dist && rm -rf .cache && parcel public/index.html --open",
        "build": "rm -rf dist && rm -rf .cache && parcel build public/index.html"
    },
    "keywords": [
        "vue",
        "parcel",
        "parcel-bundler",
        "parceljs",
        "parcel-vue"
    ],
    "author": "{{ author }}",
    "license": "{{ license }}",
    "license": "ISC",
    "devDependencies": {
        "@vue/component-compiler-utils": "^2.3.0",
        "babel-core": "^6.26.3",
        "babel-plugin-transform-runtime": "^6.23.0",
        "babel-preset-env": "^1.7.0",
        "babel-preset-vue": "^2.0.2",
        "parcel-bundler": "^1.10.3",
        "sass": "^1.14.3",
        "vue-template-compiler": "^2.5.17"
    },
    "dependencies": {
        "vue": "^2.5.17",
        "vue-hot-reload-api": "^2.3.1"
    }
}
